{% extends 'base.html.twig' %}

{% block title %}{{ 'title.register'|trans }}{% endblock %}

{% block body %}
    {% for flashError in app.flashes('verify_email_error') %}
        <div class="alert alert-danger" role="alert">{{ flashError }}</div>
    {% endfor %}

    {% if app.user %}
        <h1>
            {{ 'message_logged_in_as'|trans }} <span>{{ app.user.username }}</span>
        </h1>
    {% else %}
        <h1>{{ 'title.register'|trans }}</h1>

        {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.nickname) }}
        {{ form_row(registrationForm.email) }}
        {{ form_row(registrationForm.password) }}
        {{ form_row(registrationForm.agreeTerms) }}

        <button type="submit" class="btn btn-lg btn-primary sign">{{ 'button_register'|trans }}</button>
        {{ form_end(registrationForm) }}

        <p>
            {{ 'message_already_have_account'|trans }}
            <a href="{{ url('app_login') }}"> {{ 'login'|trans }}</a>
        </p>
    {% endif %}
{% endblock %}