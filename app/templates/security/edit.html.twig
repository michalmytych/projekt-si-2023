{% include 'default/_form.html.twig' with {
    page_title: 'title.user_edit_profile',
    page_title_param: {'%email%': user.email},
    attr: {
        method: 'PUT',
        action: url('user_edit_profile', {'id': user.id}),
    },
    form: form,
    back_to_list_path: 'article_index',
} %}

