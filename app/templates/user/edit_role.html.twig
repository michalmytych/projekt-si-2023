{% include 'default/_form.html.twig' with {
    page_title: 'title_user_edit_role',
    attr: {
        method: 'PUT',
        action: url('user_edit_role', {'id': user.id}),
    },
    form: form,
    back_to_list_path: 'user_index',
} %}

